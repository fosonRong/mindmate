# 智伴 Mindmate — 验收报告

| 项目 | 内容 |
| --- | --- |
| 验收日期 | 2026-09-13（最新一轮：v1.0.8） |
| 验收对象 | 智伴 Mindmate v1.0.8（Windows 桌面端优先，同一二进制支持 B/S） |
| 依据文档 | [需求文档.md](./需求文档.md) v3.0 · [技术设计文档.md](./技术设计文档.md) v1.0 · [UI设计文档.md](./UI设计文档.md) v1.0 · [商业化方案.md](./商业化方案.md) v3.1 |
| 验收结论 | **FR-1 ~ FR-7 全部功能点（含全部 P1）、M1–M7 全部里程碑、两轮共 9 项用户反馈缺陷、第一期商业化前置任务 T1.1–T1.11 均已交付并验证**（最新一轮断言 **389 项全绿**：Rust 单元 105 / 端到端 172 / 提醒 12 / 推送 20 / 桌面端静态 46 / 国际化 24 / 词条编译 10；自动更新 1.0.6 → 1.0.7 → 1.0.8 实测升级且数据无损；界面实测通过） |

---

## 一、交付物清单

| 交付物 | 路径 | 说明 |
| --- | --- | --- |
| Windows 安装包 | `src-tauri/target/release/bundle/nsis/Mindmate_1.0.0_x64-setup.exe` | **3.20 MB**（NSIS，当前用户安装） |
| 绿色版可执行文件 | `src-tauri/target/release/mindmate.exe` | **8.46 MB**，双击即用（含托盘/热键/服务） |
| 服务器部署 | `Dockerfile` · `docker-compose.yml` · `Caddyfile.example` | B/S 异地部署，含健康检查与自动 HTTPS 反代示例 |
| 源码 | `core/`（Rust 核心） · `src-tauri/`（桌面壳） · `apps/web/`（Vue 3 前端） | 一套后端 + 一份前端产物，双端共用 |
| 测试 | `scripts/e2e_test.py` · `scripts/reminder_test.py` · `scripts/run_all_tests.py` · `core/tests/core_logic.rs` | 一键全量验收（`python scripts/run_all_tests.py`） |
| 文档 | `README.md` · `docs/需求文档.md` · `docs/技术设计文档.md` · `docs/UI设计文档.md` · `docs/验收报告.md` | 需求 → 设计 → 验收闭环 |
| UI 高保真稿 | `ui-mockups/index.html`（含设计系统与全部页面） | 设计评审用（浏览器打开） |

---

## 一之二、里程碑计划逐项验收（对照需求文档 §9）

| 里程碑 | 计划内容 | 实际交付 | 证据 | 结论 |
| --- | --- | --- | --- | --- |
| **M1 地基** | Monorepo + Rust 核心（axum + rusqlite + 迁移 + 认证）+ Web 脚手架 + Tauri 壳 | 根 `package.json` 配置 **npm workspaces**（本机无 pnpm，等价落地 Monorepo，已在 README 说明）；Rust 核心含数据模型/迁移/认证；Vue3 脚手架；Tauri 壳内嵌前端 + 同进程服务 | `npm run web:build` / `test:rust` 经 workspaces 正常执行；服务启动日志 | ✅ |
| **M2 记录核心** | 自由录入、时间线、今日/周/月视图、双进度条、每日目标、热键速记浮窗、SSE 多端同步 | 全部实现 | 端到端 §2/§3/§9；界面实测今日/周/月三视图；热键注册日志 | ✅ |
| **M3 提醒** | 服务端定时器、上下文智能提醒、睡眠唤醒补偿、待办提醒、勿扰、双端通知 | 全部实现；本轮**把睡眠唤醒补偿提取为纯函数**并补测试，解决此前"已实现但不可验证" | 提醒专项 12 项；`should_compensate`/`makeup_body` 3 项单测 | ✅ |
| **M4 待办** | 四类待办、待办中心左右分栏、日程、拖拽、逾期判定 | 全部实现 | 端到端 §4（归类/流转/逾期/改期/筛选/日程双栏）；界面实测左右分栏 + 拖拽改期 | ✅ |
| **M5 智伴 AI** | 模型配置（预设+测试连接+脱敏）、晨间简报、日报、晚安总结、SSE 流式、无 Key 降级 | 全部实现 | 端到端 §5/§6/§8；界面实测报告流式生成与降级提示条 | ✅ |
| **M6 智伴进阶** | 周报/月报、周度复盘、智伴问答、历史归档、模板编辑、邮件/Webhook 推送 | 全部实现 | 端到端 §5/§6/§7；推送专项 20 项（含真实 Webhook 投递） | ✅ |
| **M7 打磨发布** | 首次启动引导、成就体系、备份/导出、主题、桌面打包、Docker 镜像、服务器部署文档 | **本轮补齐前 2 项缺失**：① 三步首启引导（部署模式 → 每日目标 → AI 模型，可跳过）② 成就体系完善为 10 枚徽章（友好名称/图标/条件/解锁时间）+ 成就抽屉；Docker 与部署文档见下 | 引导三步实测走通并正确关闭；成就抽屉实测 10 枚徽章、3 枚已解锁、未解锁显示条件；Docker/部署文档静态校验 32 项通过 | ✅ |

> **本轮里程碑审计发现并补齐的缺口**：M7 的「首次启动引导」此前完全缺失、「成就体系」仅有 6 个原始 id 且界面展示 `first_node` 这类原始标识、「服务器部署文档」缺失（仅 README 零散说明）、M3 的睡眠唤醒补偿缺乏可验证性、M1 的 Monorepo 未配置 workspaces。均已修复。
>
> **Docker 说明**：本机无 Docker 环境，无法实际构建镜像。已对 Dockerfile（11 项）、docker-compose（5 项）、Caddyfile（3 项）、部署文档（8 项）完成结构与引用静态校验（32 项全通过），且容器健康检查命令 `--healthcheck` 已在真实运行的服务上验证退出码 0。

## 二、非功能指标达标情况

| 指标（需求文档 1.6 / 8） | 目标 | 实测 | 结论 |
| --- | --- | --- | --- |
| 安装包体积 | ≤ 10 MB | **3.20 MB** | ✅ 达标（优于目标 3 倍） |
| 便携版体积 | — | 8.46 MB | ✅ |
| 运行时内存 | — | **33.8 MB**（对比 Electron 通常 300–500MB） | ✅ 优 |
| 前端产物 | — | 342 KB（gzip ≈ 95 KB） | ✅ |
| 冷启动 | < 3 秒 | 服务监听约 0.03s；桌面壳就绪 < 1s | ✅ |
| 数据存储 | 本地 SQLite | `%APPDATA%\Mindmate\data.db`（WAL，实测 4KB 起） | ✅ |
| 双端一致性 | 桌面 = 浏览器 | 同一 url/同一数据/同一 API，实测两端同步 | ✅ |
| 离线可用 | 除 AI 外全离线 | 本地模式完全离线（AI 自动降级本地模板） | ✅ |

---

## 三、功能点逐项验收（对照需求文档 FR-1 ~ FR-7）

### FR-1 快速记录

| 需求 | 验收方式 | 证据 | 结论 |
| --- | --- | --- | --- |
| FR-1.1 自由录入，一次提交 = 一个节点，无模板 | 接口 + 界面实测 | 连录 3 条 → 时间线 3 条、状态栏「今日 3 条」、数据库 `nodes` 3 行 | ✅ |
| FR-1.2 时间线展示（时刻+内容+标签，可编辑删除） | 界面实测 | 截图可见时刻 `19:56`、标签「工作」、悬停「编辑/删除」 | ✅ |
| FR-1.3 桌面全局热键速记浮窗 | 启动日志 + 代码 | 日志：`全局热键已注册：Alt+Z`；`速记浮窗（decorations=false, always_on_top）` | ✅ |
| FR-1.4 浏览器页面内速记框 | 界面实测 | 顶部常驻速记框（截图），回车保存并 Toast「已记录 19:53」 | ✅ |
| FR-1.5 通知内速记 | 代码 + 事件 | 提醒事件 `action=quick_entry` → 桌面弹浮窗 / 浏览器聚焦速记框 | ✅ |
| FR-1.6 周/月聚合视图 | 接口 + 界面 | `period_stats` 按日聚合；周视图 7 列、月视图日历格 | ✅ |
| FR-1.7 补录 | 接口验证 | 补录昨日记录 → `isBackfill=true` | ✅ |
| FR-1.8 每日目标（默认 4，可改可关） | 接口 + 界面 | 默认 `daily_goal=4`；改为 5 后统计随之变化；超额显示「超额 n」 | ✅ |
| FR-1.9 内容标签 | 界面实测 | 工作/生活/健康/学习四类标签可选并按色渲染 | ✅ |
| FR-1.11 多端实时同步 | SSE 实测 | 订阅 `/stream/events` 收到 `node.created`；桌面录入后浏览器同步 | ✅ |

### FR-2 周期视图与进度

| 需求 | 验收方式 | 证据 | 结论 |
| --- | --- | --- | --- |
| FR-2.1 今日双进度条 | 界面截图 | 「记录进度 3/4 条」「待办完成 2/4」并排渲染 | ✅ |
| FR-2.2 周视图（按日聚合 + 完成率环） | 代码 + 接口 | `/stats/period` 返回 days 数组（节点数/摘要/待办完成） | ✅ |
| FR-2.3 月视图（日历格 + 摘要） | 界面截图 | 日历格显示节点数徽标（12 日显示 3）与摘要 | ✅ |
| FR-2.4 连续记录展示 | 界面截图 | 今日页「🔥 连续记录 1 天」、侧栏 🔥1 | ✅ |
| FR-2.5 进度分级配色 | 代码 | `<30% 灰 / 30–70% 黄 / >70% 绿` | ✅ |

### FR-3 待办管理

| 需求 | 验收方式 | 证据 | 结论 |
| --- | --- | --- | --- |
| FR-3.1 四类自动归类 | 接口实测 | 今日/本周/本月/日程 四类均验证（含具体时间→日程） | ✅ |
| FR-3.2 完整字段 | 接口 + 界面 | 标题/描述/截止日期/时间/提醒/优先级/标签/状态 | ✅ |
| FR-3.3 状态流转 + 逾期自动标记 | 接口实测 | 完成→已完成（记录 completedAt）；撤销→待处理；过期→已逾期 | ✅ |
| FR-3.4 筛选与搜索 | 接口实测 | 按分类筛选、关键词搜索（中文）均正确 | ✅ |
| FR-3.5 自动归类规则 + 手动修改 | 接口实测 | 改期后自动重算分类；支持显式指定 | ✅ |
| FR-3.6 拖拽改期 | 界面 + 接口 | 列表项可拖至日历某日 → `PATCH dueDate`；日历为放置目标 | ✅ |
| FR-3.8 智能排期建议 | 接口实测 | `/ai/replan` 无 Key 时给规则建议，有 Key 时给 AI 建议 | ✅ |

### FR-4 提醒系统（上下文智能）

| 需求 | 验收方式 | 证据 | 结论 |
| --- | --- | --- | --- |
| FR-4.1 频率可配（每小时/2小时/4小时/自定义） | 界面 + 接口 | 设置页可选，值持久化到 `remind_freq_minutes` | ✅ |
| FR-4.2 生效时段 | 提醒专项测试场景 5 | 时段外静默（窗口内零提醒） | ✅ |
| FR-4.3 上下文智能规则 | 提醒专项测试场景 1/2/3 | 关怀（streak=0）、常规（含 `已录 0/4 条` 进度文案）、逾期加压（含标题与计数）**均真实触发**；达标静默（场景 3 零提醒） | ✅ |
| FR-4.5 待办到期提醒 | 界面实测 | 截图中真实弹出「待办即将到期：健身房 1 小时（截止 19:30）」 | ✅ |
| FR-4.6 全局与分类开关 | 界面 + 接口 | 记录/待办/简报/晚安/复盘各自开关 | ✅ |
| FR-4.7 常驻可靠性 | 启动日志 | 桌面：`桌面壳就绪（托盘 + 热键 + 通知）`；服务器：服务端定时器 | ✅ |
| FR-4.9 勿扰模式 | 提醒专项测试场景 6 | 勿扰时段内零提醒 | ✅ |
| FR-4.8 睡眠唤醒补偿 | 代码（tick 间隔检测 + 聚合文案） | 间隙 > 2×频率即聚合补发「错过了 n 次记录提醒」 | ✅ 已实现 |
| **FR-4.10 推送渠道扩展（邮件/Telegram/企业微信 Webhook）** | 推送专项测试（本地 Webhook 接收器） | 3 渠道配置与凭据安全存储；**测试推送真实到达接收器**（校验 `msgtype=text` 与文案）；**提醒触发后自动经渠道投递**（接收内容与提醒标题一致）；未启用不推送；配置缺失/连接失败均返回可读错误且不影响服务 | ✅ |
| 提醒设置变更即时生效 | 提醒专项测试（隐含） | 修改设置后无需等待旧频率窗口，立即按新规则评估 | ✅ |

### FR-5 AI 能力

| 需求 | 验收方式 | 证据 | 结论 |
| --- | --- | --- | --- |
| FR-5.1 双预设（GLM/DeepSeek）+ 自定义 | 接口 + 界面截图 | 6 个预设返回；设置页两张推荐卡（GLM 带「免费」徽标选中态）；Base URL 正确 | ✅ |
| FR-5.2 Key 只存系统安全存储、不回读 | 接口实测 | `/ai/config` 返回 `hasKey` 布尔，无 `apiKey` 字段 | ✅ |
| FR-5.3 测试连接 | 接口 | `/ai/test` 返回延迟与模型回显 | ✅ |
| FR-5.4 无 Key 降级 | 接口 + 界面 | 六类生成全部返回本地模板内容，界面显示黄色降级提示条 | ✅ |
| FR-5.5 晨间简报 | 接口 + 界面 | 生成成功（127 字），今日页卡片展示 | ✅ |
| FR-5.6 日报（含已完成待办与当前进度） | 界面实测 | 生成后渲染：今日完成（3 条带时刻）、当前进度（记录 3/4；待办 2/6，完成率 33%）、今日待办、需要注意 | ✅ |
| FR-5.7 周报（按日聚合） | 接口 | 生成成功，含按日分节 | ✅ |
| FR-5.8 月报 | 接口 | 生成成功 | ✅ |
| FR-5.9 晚安总结 | 接口 | 生成成功（161 字） | ✅ |
| FR-5.10 周度智能复盘 | 接口 | 生成成功（180 字） | ✅ |
| FR-5.11 智伴问答（本地检索） | 接口 | 问「今天记了什么」→ 命中本地记录并回答；历史落库 | ✅ |
| FR-5.13 Prompt 模板可自定义 | 接口实测 | 7 类模板返回内置值；保存自定义后 `customized=true`；可恢复默认 | ✅ |
| FR-5.14 复制/导出/归档 | 界面 | 「复制 MD」「导出」按钮；报告自动归档（`/reports` 可查） | ✅ |
| FR-5.16 脱敏 | 单元测试 | 手机号/身份证/邮箱/链接均替换为 `[隐私]`/`[链接]`；普通文本不受影响 | ✅ |
| FR-5.18 流式输出 | 接口 + 界面 | SSE `delta` 逐段推送；界面打字机光标 | ✅ |

### FR-6 / FR-7

| 需求 | 验收方式 | 证据 | 结论 |
| --- | --- | --- | --- |
| FR-6.1 连续记录 | 界面 + 统计接口 | 连续记录天数徽标；streak 计算含「今天未记则从昨天起算」语义 | ✅ |
| FR-6.2 成就体系（10 枚徽章 + 抽屉） | 接口 + 单测 + 界面实测 | 徽章目录含需求列出的全部 7 类（首次速记/连续 3-7-30 天/单日 10 条/连续 3 天超额/首次报告/首次复盘/当月 25 天）共 10 枚，均带名称/图标/描述/条件；自动解锁 + 幂等；今日页展示友好徽章，抽屉实测 10 枚、3 枚已解锁、未解锁显示条件、已解锁带日期 | ✅ |
| 首次启动引导（M7） | 界面实测 | 全新数据首次打开自动弹出三步引导（使用方式 → 每日目标 → AI 模型可跳过），实测逐步走通并写回 `onboarded` 标记；再次启动不再打扰 | ✅ |
| **FR-6.4 月度小结卡** | 接口 + 单元测试 + 界面实测 | `GET /stats/monthly` 返回「记录天数 / 完成待办 / 最长连续 / 节点数 / 日均 / 月底标记」；月视图顶部卡片实测显示「1/30 天 · 1/4 件 · 1天 · 4条 · 日均 4」；3 项单元测试覆盖连续天数、月底标记、跨月隔离 | ✅ |
| FR-7.1 提醒设置 | 界面截图 | 频率/时段/勿扰/开关齐全 | ✅ |
| FR-7.2 每日目标 | 界面 + 接口 | 默认 4，可改可关 | ✅ |
| FR-7.3 AI 配置 | 界面截图 | 预设卡 + Base URL + Key + 模型 + 高级参数 + 测试连接 | ✅ |
| FR-7.4 部署模式与访问密码 | 接口实测 | 服务器模式强制登录；未登录 401；错误密码 401；首次设置密码；JWT 登录 | ✅ |
| FR-7.5 备份导出/导入 | 接口实测 | 导出含节点/待办/设置/报告；清空后导入数据完整 | ✅ |
| **FR-7.6 全量数据导出为 Markdown 归档（按周期分文件）** | 接口 + 界面实测 | 返回合法 ZIP（`application/zip` + 下载文件名），内含 `README.md` 索引、`daily/YYYY-MM-DD.md`、`weekly/YYYY-Www.md`、`monthly/YYYY-MM.md`、`todos.md`、`reports/*.md`；页面内实测下载 2.6KB ZIP 成功 | ✅ |
| FR-7.7 主题三态 | 界面实测 | 切换深色后 `data-theme=dark` 生效（截图验证），可恢复跟随系统 | ✅ |

### 架构能力（需求文档第 2 章）

| 需求 | 验收方式 | 证据 | 结论 |
| --- | --- | --- | --- |
| B/S + C/S 双架构 | 实测 | 桌面进程内嵌服务 + 浏览器访问 `http://127.0.0.1:17801` 同一界面/数据 | ✅ |
| 三种部署模式 | 实测 | `--mode local`（默认）/ `--mode lan`（需密码）/ `--mode server --headless`（Docker） | ✅ |
| 端口冲突顺延 | 代码 | 17801 起自动探测至 17820 | ✅ |
| 容器健康检查 | 实测 | `--healthcheck` 退出码 0（探针可用） | ✅ |

---

## 四、测试执行记录（可复现）

```bash
# 一键全量验收（需先启动服务）
python scripts/run_all_tests.py
```

| 测试套件 | 项数 | 结果 |
| --- | --- | --- |
| Rust 单元测试（含协议/推送/归档/月度小结/成就/睡眠补偿/跨零点时段/问答检索/迁移链/首见证据） | 90 | ✅ 全部通过 |
| 端到端接口验收（含月度小结、归档、缓存、成就、引导、缺陷回归、首见证据） | 156 | ✅ 全部通过 |
| 提醒系统专项验收（FR-4） | 12 | ✅ 全部通过 |
| 推送渠道专项验收（FR-4.10，含真实 Webhook 投递） | 20 | ✅ 全部通过 |
| 桌面端静态验收（IPC 授权 / 浮窗生命周期 / 更新与发布通道） | 44 | ✅ 全部通过 |
| **断言总数** | **322** | ✅ 全部通过 |
| 前端生产构建 | — | ✅ 通过 |

**单元测试覆盖**：智能提醒规则（7 项：常规/达标静默/逾期加压/关怀/勿扰/时段外/频率未到）、提醒文案规范、时间解析、待办归类（5 项）、逾期判定（3 项）、提醒时刻计算、脱敏（2 项）、周期范围（3 项）、模板渲染（2 项）、数据层（9 项：节点/补录/软删/检索/待办流转/逾期刷新/改期归类/默认设置/备份导出）、降级报告（3 项）。

**接口验收覆盖**：认证流程（未登录拦截/错误密码/设置密码/登录/JWT）→ 记录全生命周期 → 四类待办归类 → 逾期 → 统计进度 → 六类 AI 报告（含降级）→ 智伴问答 → 设置持久化 → 模板自定义 → AI 预设 → SSE 事件流 → 备份恢复 → 边界校验（空内容/空标题/404）。

**提醒专项覆盖**：关怀提醒真实触发与文案、常规提醒含进度文案与点击行为、逾期加压含标题与计数、达标静默、时段外静默、勿扰静默。

---

## 五、界面实测记录

在真实浏览器中加载应用并与后端交互，验证结果：

| 页面 | 实测项 | 结果 |
| --- | --- | --- |
| 今日 | 晨间简报卡、双进度条、速记录入（3 次）、时间线、今日待办、逾期区、徽章、状态栏 | ✅ 截图 |
| 待办中心 | 四类分组（今日/本月/日程）、状态与优先级筛选、日历（节点徽标 + 待办圆点）、当日「具体日程/待办事项」双栏 | ✅ 截图 |
| 智伴 | 报告/复盘/问答 Tab、报告类型切换、日期选择、**生成日报（流式）**、降级提示、复制/导出 | ✅ 截图 |
| 设置 | AI 模型页：6 个预设卡（GLM 免费选中态）、Base URL、Key、模型、测试连接、7 类 Prompt 模板 | ✅ 截图 |
| 主题 | 深色主题切换（`data-theme=dark`）全站生效 | ✅ 截图 |
| 提醒 | 页面内提醒横幅真实弹出（待办到期） | ✅ 截图 |

**实测中发现并修复的缺陷**（体现端到端验证价值）：

| # | 缺陷 | 根因 | 修复 |
| --- | --- | --- | --- |
| 1 | 默认设置全部未写入数据库 | `settings.updated_at NOT NULL` 而种子插入未提供该字段，SQLite `OR IGNORE` 静默跳过 | 种子插入补齐 `updated_at` |
| 2 | 备份导出接口挂起（死锁） | `export_all` 持锁期间调用同样加锁的 `all_settings`/`list_reports`（Mutex 不可重入） | 释放锁后再调用 |
| 3 | 中文全文检索无结果 | SQLite FTS5 默认分词器（unicode61）不切分中文 | 改用 LIKE 检索（个人数据量足够且中文友好） |
| 4 | 修改提醒设置后长时间不再提醒 | 提醒计时窗口未随设置变更重置 | 监听 `settings.updated` 事件重置计时，即时按新规则评估 |
| 5 | 同一条记录在时间线显示两次 | 服务端在响应前广播事件，前端「本地插入 + SSE 事件」重复添加 | 插入前按 id 幂等去重（节点与待办） |
| 6 | 应用升级后界面仍用旧资源（月度小结卡不显示） | 前端资源无缓存策略，WebView/浏览器复用旧 JS | `index.html` 与 SPA 回退页 `no-cache`；`/assets/*` 哈希资源 `immutable` 长缓存（并纳入自动化用例） |
| 7 | 简报/晚安/复盘走降级路径时「汇报达人/复盘专家」徽章不解锁 | 通用 SSE 降级分支未做成就评估（报告落库在 handler 内、评估只在报告 handler 内） | 在 `ai_stream_from` 的降级分支统一调用 `achievements::check_all` |
| 8 | `npm run test:rust` 在 npm 子进程中报「cargo 不是命令」 | npm 子进程不继承手工添加的 PATH（cargo 为用户级安装） | 新增 `scripts/run-cargo.mjs` 跨平台包装器，自动补齐 `~/.cargo/bin` |
| 9 | **配好 AI 后调用仍失败**：智谱返回「temperature 参数非法：限制小数点[2]位」 | `temperature` 为 f32，serde_json 转 f64 时暴露表示误差，实际发送 `0.699999988079071` | 新增 `normalize_temperature`（保留 2 位小数 + 限幅 0–2），请求体与设置落库均归一化；加 2 项单测断言请求体为 `0.30000000000000004`-free 的两位小数 |
| 10 | AI 生成的日报在历史里查不到（归档类型错乱） | 流式路径用 tag 推导归档 `(type, period)`，日报 tag 是日期 → 存成 `type="2026-09-12"` | 引入 `StreamPlan` 显式声明落库目标，落库统一到 `ai_stream_from` |
| 11 | 智伴问答的 AI 回复不落库（刷新即丢） | 助手消息只在降级分支写入 `chat_messages`，AI 分支遗漏 | 同上，统一在流式结束时落库 |
| 12 | 提醒生效时段/勿扰规则**跨零点失效**（如 22:00–02:00 永不触发） | 时段判定用 `now >= start && now <= end`，跨零点时 end < start | 新增 `in_window`（`start > end` 视为跨午夜）与 `in_dnd_rules` 纯函数 + 5 项单测 |

---

---

## 五之二、用户反馈缺陷修复记录（6 项，均已实测验证）

| # | 反馈 | 根因 | 修复 | 验证证据 |
| --- | --- | --- | --- | --- |
| 1 | 模型提供商 Base URL 需支持自定义；保存后切换模型不应恢复默认地址 | 切换预设卡片时会用预设默认值直接覆盖 `baseUrl`；且地址与模型耦合 | 预设选择改为「仅当地址为空或等于某预设默认值时才填入」；新增「恢复该提供商默认地址」「使用 Anthropic 兼容地址」按钮；新增协议选择器（auto/openai/anthropic）与「将使用的协议」提示 | e2e §10.10 共 12 项断言（自定义地址保存 / 切模型不变 / 切提供商不变 / 重读仍为自定义值 / 显式协议覆盖 / 两个 Anthropic 地址识别）；界面实测：地址可编辑、两按钮与协议提示就位 |
| 2 | 设置开机自启异常报错 | 依赖 `tauri-plugin-autostart` + JS 插件，错误信息不可读且受权限配置影响 | 改为**自研实现**：Windows 写用户注册表 Run 键、macOS 写 LaunchAgent、Linux 写 autostart desktop 文件；提供 `autostart_status` / `autostart_set` 命令；失败时以「读回状态」判定并返回可读中文错误 | Rust 单测：开启 → 读回 → 关闭 → 再读回（含幂等）；界面实测按平台显示可用性提示 |
| 3 | 待办只显示标题，看不到详细说明 | `description` 字段已有但前端未渲染 | `TodoItem` 增加说明行（两行折行 + 悬停全文） | 界面实测：5 条待办说明正常显示；e2e §10.11 验证说明的保存 / 列表返回 / 编辑持久化 |
| 4 | 日程需要显示待办标题 | 日历格仅渲染圆点，未渲染标题 | `CalendarMonth` 新增 `todoTitles`，格内直接显示「标题」或「时刻 + 标题」，超出以 `+n 项待办` 归纳 | 界面实测：格内显示「· 修复登录 bug」「· 10:00 需求评审会」「· 14:00 写技术方案」 |
| 5 | 月度日程需显示待办标题、支持筛选月份，点击「本月」无反应 | 待办中心日历固定在当月、无任何月份控件；月视图缺月份选择器 | 两处均加入 ◀ / ▶ / `input[type=month]` 选择器 / 「本月」按钮，切换即重新拉取该月统计与待办 | 界面实测：月份选择器值 `2026-09`，点击「下个月」后变为 `2026-10`；e2e §10.12 验证按月区间取标题、跨月不串数据、月度小结按月区分 |
| 6 | 已配置 AI，晨间简报仍提示「配置 AI 模型」 | 提示只看历史报告的 `isAi` 标记，不看当前配置；旧模板报告会一直触发提示 | 新增 `app.aiReady`（读 `/ai/config`），提示分流：未配置 → 「去配置」；已配置但旧报告 → 「AI 模型已配置，点此重新生成」 | 界面实测：生成后无任何配置提示，产出真实 AI 简报（内容含「🌟 昨日遗留 / 今日重点 / 鼓励」，非本地模板） |

### 附带修复（测试过程中主动发现）

| 问题 | 说明 |
| --- | --- |
| AI 双协议支持 | 用户提供的地址为 Anthropic 兼容端点（`/v1/messages`），原实现仅支持 OpenAI 兼容协议会 404 → 新增协议识别与 Anthropic 客户端（`x-api-key` + `anthropic-version`、SSE `content_block_delta` 解析、非流式 `content[]` 拼接） |
| temperature 浮点精度 | 见缺陷表 #9 |
| 报告归档类型 / 问答落库 | 见缺陷表 #10 / #11 |
| 跨零点时段与勿扰 | 见缺陷表 #12 |

### 真实 AI 调用验证（使用本机已配置的智谱 GLM Key）

| 项目 | 结果 |
| --- | --- |
| OpenAI 兼容端点 `https://open.bigmodel.cn/api/paas/v4` | ✅ 连接成功，回显「你好」（538ms） |
| Anthropic 兼容端点 `https://open.bigmodel.cn/api/anthropic` | ✅ 连接成功，回显「你好」（736ms） |
| 真实生成的报告 | 日报 257 字 / 周报 343 字 / 月报 416 字 / 晨间简报 90 字 / 晚安总结 161 字 / 周度复盘 466 字，全部由模型生成（非模板降级） |

## 五之三、第二轮用户反馈修复记录（3 项，均已实测验证）

> 本轮三个问题同源：桌面窗口加载的是**本机 HTTP 页面**（`http://127.0.0.1:<端口>`），在 Tauri 2 中属于 **remote 来源**。
> 项目既未在 capability 的 `remote.urls` 放行该来源，也未在 `build.rs` 声明应用自身命令的 app manifest，
> 于是**前端所有 `invoke` 都被 ACL 拒绝**（`Command xxx not allowed by ACL`）——
> 这是「开机自启报错」与「速记浮窗白屏、Esc 关不掉」共同的根因。

| # | 反馈 | 根因 | 修复 | 验证证据 |
| --- | --- | --- | --- | --- |
| 1 | 设置开机自启仍然报错（上一轮未修复） | 桌面端 `invoke('autostart_set')` 被 ACL 拒绝：capability 缺 `remote.urls`；应用自身命令也未在 `build.rs` 的 app manifest 中声明 → `invoke.acl` 为空即拒绝 | ① capability 增加 `"remote": {"urls": ["http://127.0.0.1:*", "http://localhost:*"]}`（端口会因占用而自增，不能写死）；② `build.rs` 改用 `try_build` + `AppManifest::new().commands([...])` 声明 5 个命令，capability 引用自动生成的 `allow-*` 权限；③ `open_quick_entry` 返回布尔值，前端据此跳过浏览器兜底 | 实测（已重装到 `%LOCALAPPDATA%\Mindmate`）：点击开关 → toast「已开启开机自启（登录后自动启动智伴，提醒持续生效）」，注册表 `HKCU\...\Run` 出现 `Mindmate = "...\mindmate.exe"`；再点 → 键值被移除、提示已关闭。**修复前同一操作**：toast「设置开机自启失败：Command autostart_set not allowed by ACL」 |
| 2 | Alt+Z 速记浮窗按 Esc 关不掉、白屏 | `Quick.vue` 的 `closeWindow()` 调用了 `window.close()`——因为 `close_quick_entry` 同样被 ACL 拒绝，代码走了浏览器兜底。Windows/WebView2 上 `window.close()` 会**销毁网页视图**，只剩一个无内容、无按钮、Esc 无效的无边框置顶白窗；此后每次 Alt+Z 又会在已损坏的视图上注入脚本，触发 `WebView2 error 0x8007139F` | ① 彻底删除 `window.close()`，收起只走 `close_quick_entry`（隐藏窗口，WebView 保活）；② 浮窗 `CloseRequested` 一律降级为隐藏（`api.prevent_close()`），窗口永不销毁；③ 去掉未就绪即失败的 `eval` 注入，改由页面自身监听 `focus` / `visibilitychange` 复位并聚焦；④ Alt+Z 与托盘「速记」改为**切换**（可见即收起）——页面异常时的兜底逃生口 | 复现：旧构建下 Alt+Z → 写入内容回车 → 出现无法关闭的白窗、Esc 无反应，再次 Alt+Z 日志报 `0x8007139F`（本机复现，与用户反馈一致）。修复后同流程：浮窗正常渲染「⏺ 速记 / 今日 n/4」→ 保存后自动收起 → 再按 Alt+Z 内容仍正常渲染（旧代码此处已是白屏）→ 再按 Alt+Z 收起 → 点浮窗内「关闭」窗口隐藏；**日志 0 条 WebView2 错误**（修复前 6 条） |
| 3 | 今日记录节点文字上方空白过多，不美观 | 标签行 `.tl-meta` 即使没有标签也照常渲染，撑出 20px 空行，叠加 8px 段间距，正文上方共 43px 留白 | 标签行改为条件渲染 `v-if="node.tags.length || node.isBackfill"`；悬停「编辑/删除」移出文档流（绝对定位于气泡右上），不再参与布局 | 浏览器实测（同一构建，`getBoundingClientRect` 量取）：无标签节点 正文上方间距 **43px → 11px**、气泡高度 76px → 44px；含标签节点标签行仍正常渲染（20px 行高、两枚标签 chip 正常） |

### 本轮新增回归护栏

| 项 | 说明 |
| --- | --- |
| `scripts/desktop_test.py`（29 项） | 桌面端静态验收，已纳入 `run_all_tests.py`。守护：capability 放行 remote 来源、app manifest 命令清单与 `allow-*` 权限、前端不存在 `window.close()`、浮窗生命周期（`prevent_close` / 失焦收起 / 热键切换 / 无 `eval` 注入） |
| 权限类错误不再静默 | `build.rs` 用 `try_build`，capability 权限写错在**构建期**即报错；运行期 ACL 拒绝会在界面弹出可读中文提示，不再「点了没反应」 |

## 六、遗留与后续建议

| 项 | 状态 | 说明 |
| --- | --- | --- |
| AI 真实调用链路 | 未实测（无可用 API Key） | 代码路径完整（OpenAI 兼容协议、SSE 解析、重试、超时）；无 Key 降级路径已全面验证。配置 Key 后即可用 |
| 局域网/服务器模式 HTTP | 已按模式实现 | 生产部署建议经 Caddy 反代启用 HTTPS（已附 `Caddyfile.example`，含 SSE 免缓冲配置） |
| 移动端浏览器适配 | 已实现响应式（底部 TabBar / 单列） | 未在真机实测 |
| macOS / Linux 打包 | 代码支持（未在对应平台构建） | 需在目标平台执行 `tauri build` |
| Docker 镜像实际构建 | 未执行（本机无 Docker） | Dockerfile/compose/Caddyfile 与部署文档已完成 32 项静态校验；健康检查命令已在真实服务验证 |
| P2 项（重复待办、语音速记、SQLCipher 等） | 未实现 | 需求文档中已标注为 P2（远期可选） |
| 开机自启开关 | ✅ 已实现并实测 | 设置页「提醒」区开关，桌面端调用自研的 `autostart_status` / `autostart_set`（Windows 写注册表 Run 键）；实测开关能真实写入/移除 `HKCU\...\Run`。浏览器端置灰并提示仅桌面可用。**未验证**：重启电脑后是否真的自动拉起（需用户重启一次确认） |

---

## 七、结论

按 [需求文档 v3.0](./需求文档.md) 的功能需求 **FR-1 ~ FR-7 全部完成开发并通过验证**，架构需求（B/S + C/S 双架构、三种部署模式）已实现，非功能指标（安装包 3.20MB、内存 33.8MB、本地数据、离线可用）达标。

**总计验证项：90（单元）+ 156（接口）+ 12（提醒）+ 20（推送）+ 44（桌面端静态）= 322 项，全部通过；界面实测覆盖 9 个页面/区块；两轮实测累计发现并修复 12 个真实缺陷；M1–M7 里程碑逐项达标。**

**本轮新增（对齐验证器指出的未覆盖项）**：FR-4.10 推送渠道（邮件/Telegram/企业微信，含真实投递验证与投递链打通）、FR-6.4 月度小结卡（接口 + 单元 + 界面）、FR-7.6 Markdown 归档导出（ZIP 按周期分文件，单元 + 接口 + 页面内实测下载）、设置页开机自启开关。

---

## 八、v1.0.8 轮次验收（用户反馈缺陷修复 + 第一期收尾）

### 8.1 本轮修复的缺陷

| # | 现象 | 根因 | 修复与护栏 |
| --- | --- | --- | --- |
| 1 | **设置页点「推送渠道」整块白屏**（用户截图确认，其余分区同样会触发） | 词条 `在 @BotFather … @userinfobot …` 中未转义的 `@` 被 vue-i18n 当作「链接消息」语法。**开发模式只告警，生产构建抛 `SyntaxError: 10`（INVALID_LINKED_FORMAT）中断整页渲染** —— 因此只在打包后复现 | 改写该词条；新增 `scripts/i18n_compile_test.mjs` 用 vue-i18n 同款编译器逐条编译四语 **1628 条**词条（key + value），构建前拦截；运行时 `$t` 改容错实现 `safeT`（失败只回退原文）；新增页面级错误边界 `SafeView`（异常渲染可重试卡片而非空白） |
| 2 | 底部状态栏版本号恒为 `v1.0.0` | 模板里写死的字面量 | 改为读真实版本（桌面端取应用版本，浏览器端取内核 `/healthz`）；并把 core / app / 桌面二进制 / web / 工作区**五处版本号一致性**纳入静态验收 |
| 3 | 英文界面露出中文提示 | 4 处 toast 用模板字符串绕过 i18n | 全部改走 `t()`；验收脚本增加「模板字符串 toast」检测 |

### 8.2 本轮新增功能（T1.6 模型配置引导）

| 项 | 实测证据 |
| --- | --- |
| 三步向导（选厂商 → 领 Key → 粘贴并测试） | 浏览器与桌面端实测：三个步骤卡片、已配置标记、高级设置折叠均正常 |
| 「去申请 API Key」外链 | 浏览器分支：实测打开 `https://open.bigmodel.cn/login?redirect=%2Fusercenter%2Fapikeys`；桌面分支：`POST /api/v1/system/open-url` 返回 `{opened:true}` 且系统浏览器确实弹出厂商页面 |
| 按状态码排错（9 类） | 失败路径实测：选本地模型 → `✗ 连接失败 · llama3.2` + 「网络不通或连接超时：…」+ 厂商原始返回；成功路径实测：`✓ 连接成功 · 延迟 671ms · 模型 glm-4-flash` + 模型回复「你好」（用真实 Key） |
| Ollama 本地检测 | 实测未安装时给出「未检测到本机 Ollama：请先安装并启动（默认端口 11434）」而非报错；已装时列出模型并自动选中 |
| 隐私说明 | 界面明示「Key 只存本机系统安全存储（Windows 凭据管理器），界面不回读；AI 请求由本机直连厂商」 |
| 外链安全 | 非 http/https、含空白/引号类字符一律 400 拒绝（端到端 + 单元测试双重覆盖）；仅本地模式开放该接口，局域网/服务器模式拒绝 |

### 8.3 本轮断言总数（389 项全绿）

| 套件 | 项数 |
| --- | --- |
| Rust 单元测试 | 105 |
| 端到端接口验收 | 172 |
| 提醒系统专项 | 12 |
| 推送渠道专项 | 20 |
| 桌面端静态验收 | 46 |
| 国际化专项 | 24 |
| 国际化消息编译（防白屏，新增） | 10 |
| **合计** | **389** |

### 8.4 本轮实测记录

- **升级链路**：1.0.6 → 1.0.7 → 1.0.8 连续升级实测，老用户取证（`.first_seen`：`2026-09-13 09:36:35`、验签 `true`）与 39 项设置完整保留。
- **白屏回归验证**：先在**生产产物**上注入同类非法词条，确认原症状精确复现（`SyntaxError: 10`）；修复后再注入，页面正常渲染、回退显示原文、控制台留下明确日志 —— 证明修复与兜底都在真实打包产物上生效。
- **界面实测**：设置页七个分区逐个切换均正常（点击耗时 63–172ms，无白屏、无控制台报错）。
- **已知观察项**：端到端套件在多套件并发时出现过 1 次偶发失败（162/163），单跑与后续 3 次全量运行均全绿，暂记为环境竞争观察项。

### 8.5 本轮结论

用户反馈的「设置页点击菜单卡顿甚至白屏」缺陷已定位到**词条语法 → 生产构建抛错 → 整页渲染中断**这一根因并修复，且补上了「构建前拦截 + 运行时不中断 + 页面级错误边界」三层防线；第一期商业化前置任务至此全部完成。

---

## 九、v1.0.9 轮次验收（用户反馈两个缺陷）

### 9.1 缺陷一：智谱「去申请 API Key」页面 404

| 项 | 内容 |
| --- | --- |
| 现象 | 点「去申请 API Key」后打开的智谱页面报 404 |
| 根因 | 用的是旧路径 `open.bigmodel.cn/usercenter/apikeys`：它能跳到登录页（所以 curl 看是 200），**登录后落到不存在的路由才 404** |
| 修复 | 改用官方文档给出的 `bigmodel.cn/usercenter/proj-mgmt/apikeys`；通义百炼同步改为直达 API Key 页 `?tab=model#/api-key`；界面补一句兜底提示「页面打不开？登录厂商官网后，在控制台里找 API Keys」 |
| 护栏 | 单测锁住五家厂商的申请页地址（含"别再改回旧路径"的注释），地址被改错会在单测里失败 |

### 9.2 缺陷二：从其它页面切到设置页卡顿很久（桌面端）

| 项 | 内容 |
| --- | --- |
| 现象 | 桌面端点「设置」要卡很久才切换过去（浏览器端不卡） |
| 根因 | 查询开机自启状态的 Tauri 命令是**同步命令**，同步命令在 **WebView 主线程**执行；而它当时靠**启动 `reg.exe` 子进程**读注册表。实测该命令 IPC 往返 **944 / 1013 / 895 ms**，即整段都冻在主线程上。浏览器端 `isDesktop()` 为假、根本不走这条路径，所以只有桌面端复现 |
| 修复 ① | 命令改 `async` + `spawn_blocking`：阻塞调用彻底离开主线程 |
| 修复 ② | Windows 侧改用**进程内注册表 API（winreg）**，不再启动子进程：同一命令往返从 **944ms 降到 11ms**（约 100×） |
| 修复 ③ | 设置页数据从 5 次串行往返改为并行拉取；自启状态改为后台填充、不阻塞页面 |
| 护栏 | 桌面端静态验收新增：涉及进程/注册表 IO 的命令必须 async + 用 spawn_blocking；自启读取不得再用 `reg.exe`（两条守卫都做过反向自检） |

### 9.3 实测对比（真实桌面运行时，WebView2）

测量方法：接 WebView2 远程调试端口，在页面里挂 10ms 心跳，取两次 tick 的最大间隔作为"主线程冻结时长"。
测量手段自检：注入一次 150ms 同步阻塞 → 测得 **150ms**（能看见冻结）。

| 测量项 | 改前（1.0.8：同步 + reg.exe） | 改后（1.0.9：async + winreg） |
| --- | --- | --- |
| `autostart_status` IPC 往返 | 944 / 1013 / 895 ms | **11 / 7 ms** |
| 该调用期间主线程最大冻结 | ≈ 往返时长（同步命令在主线程） | **12–13 ms**（IPC 基线，无冻结） |
| 进入设置页（点击 → 设置页可用） | — | **75 ms**，期间最大冻结 8 ms |
| 设置页接口耗时 | 串行累加 | 并行，18–30 ms 一批 |

> 说明：改前的"主线程冻结"由两条独立事实确定——(1) Tauri 的同步命令在主线程执行；
> (2) 同一份工作（读注册表）实测耗时 944–1013ms。改后在同一构建内实测冻结仅 12–13ms，
> 且对照的同步命令 `open_quick_entry` 往返仅 4–6ms（窗口操作本身很快，不构成卡顿）。

### 9.4 本轮断言（397 项全绿）

Rust 单元 106 · 端到端 172 · 提醒 12 · 推送 20 · 桌面端静态 53 · 国际化 24 · 词条编译 10 = **397**

> 本轮另修一处**测试自身的缺陷**：推送专项与端到端套件里有"默认状态"类断言直接依赖环境残留，
> 上一次运行若中途异常退出就会误报。已改为**先归零再断言**（两处），使套件可重复运行。

---

## 十、v1.0.15 轮次验收（用户反馈：日报显示源码 + 设置页侧栏固定）

| # | 反馈 | 根因 | 修复 | 实测 |
| --- | --- | --- | --- | --- |
| 1 | 日报内容显示 Markdown 源码（周报正常） | 模型**偶发**把整份报告包在 ` ```markdown … ``` ` 里输出；日报那次被包住、周报那次没有。渲染器把整份报告当成一个代码块 | 双层修复：① 渲染层 `unwrapMarkdownFence`——仅当内容整体被 markdown/md/裸围栏包住时剥掉（```js 等真代码块不受影响），**历史已落库的围栏报告也直接正常显示**，无需迁移数据；② 六类报告模板追加「不要用代码块包裹」提示词约束（从源头降概率） | 用用户实际那条带围栏的日报验证：渲染为 h1 + 4 个 h2 小节 + 7 个列表项，非代码块；新增 `markdown_test.mjs` 17 项断言（纳入全量验收），Rust 侧新增模板约束断言 |
| 2 | 设置页内容下滑时左侧菜单跟着滚走 | 侧栏为普通文档流 | `.set-menu` 改 `position: sticky`（滚动容器为 `.page-body`）；窄屏单列布局时取消固定（避免菜单挡内容） | 实测内容滚动 666px，侧栏视口位置恒定（76px）不动 |

### 本轮断言（440 项全绿）

Rust 115 · 端到端 176 · 提醒 12 · 推送 20 · 桌面端静态 66 · 国际化 24 · 词条编译 10 · **Markdown 渲染 17（新增）**

### 附带改进（同轮发布）

- 存量默认模板升级清单补齐六类报告模板（brief V1/V2 + daily/weekly/monthly/goodnight/review 旧版），
  点过「保存/恢复默认」的用户升级后自动拿到带防围栏约束的新模板。

---

## 十一、v1.0.16 轮次验收（用户反馈：周视图「＋1 更多」点击没反应）

| 项 | 内容 |
| --- | --- |
| 根因 | 「＋N 更多」只是一行**纯文本提示**，从未绑定点击事件——设计时只提示"还有更多"，没做展开 |
| 修复 | 改为真正的按钮：点击展开该日全部记录（且展开后显示**完整文本**，不再截断 22 字），再点「收起 ▴」折叠回 3 条；与月视图"点开看详情"的交互习惯一致 |
| 四语 | 新增「展开当天全部记录」词条；i18n 覆盖检查同时抓到 title 用了不存在的「收起」词条，改为复用「收起 ▴」 |
| 守卫 | 静态验收新增 3 条：「＋N 更多」必须是按钮且绑定展开事件；必须支持收起；截断只允许出现在折叠态 |
| 实测 | 浏览器实测：点「＋1 更多」→ 显示全部 4 条（含当晚新录的 23:16 一条）且长文本完整 → 按钮变「收起 ▴」→ 点击恢复 3 条 + 截断 |

### 本轮断言（443 项全绿）

Rust 115 · 端到端 176 · 提醒 12 · 推送 20 · 桌面端静态 69 · 国际化 24 · 词条编译 10 · Markdown 渲染 17

---

## 十二、v1.0.17 轮次验收（用户反馈：1.0.16 安装后无法打开）

| 项 | 内容 |
| --- | --- |
| 现象 | 弹窗「无法成功完成操作，因为文件包含病毒或潜在的垃圾软件」；1.0.15 能开，1.0.16 不能 |
| 根因 | **不是程序缺陷**：Defender 机器学习模型把 CI 构建的未签名主程序误判为 `Trojan:Win32/Bearfoos.A!ml`，安装包能装、主程序一落地就被隔离。实锤：同一提交本机构建的产物扫不出，CI 产物复现存目录即被隔离；1.0.15 产物干净 |
| 修复 | 新增 `scripts/defender_check.py`（复制到 %TEMP% 中立目录 + MpCmdRun 判定，规避排除项导致的假阴性）；CI 在上传 Windows 产物前 `--strict` 强制扫描，检出即不发；下载页新增 Windows 误报自救指引；新增 `docs/杀软误报处置.md`（含给微软 WDSI 的误报说明模板、代码签名长期方案） |
| 守卫 | 静态验收新增 9 条：自检脚本存在、中立目录扫描、退出码判据、--strict、非 Windows 跳过、CI 强制扫描、全量验收纳入、下载页指引、处置文档 |
| 隐患改进 | tauri.conf 补齐 publisher/copyright/category/homepage（VERSIONINFO 此前 CompanyName 之外全空，降低"未知发布者"观感） |
| 实测 | MpCmdRun 对 1.0.16 CI 产物稳定检出、对 1.0.15 与本地 1.0.16/1.0.17 构建判定干净；隔离的 1.0.16 主程序在排除项下正常启动（healthz 1.0.16、窗口创建成功）——程序本身无缺陷 |

### 本轮断言（452 项全绿）

Rust 115 · 端到端 176 · 提醒 12 · 推送 20 · 桌面端静态 78 · 国际化 24 · 词条编译 10 · Markdown 渲染 17

---

## 十三、v1.0.18 轮次验收（需求迭代：农历节假日 / 日程方块自适应 / 循环待办 / 我的简报+今日热点）

| 需求 | 实现 | 验证 |
| --- | --- | --- |
| 1. 日程显示农历、节假日（含休/班） | 自研 `apps/web/src/lib/lunar.ts`：1900-2049 农历压缩表（npm 源 solarlunar 实测损坏后弃用）+ 2025 官方/2026 推算调休表（休/班徽标）+ 表外年份固定规则兜底；接入月视图格、周视图卡、全局头部、日详情抽屉 | 真实锚点 11/11（历年春节/端午/中秋/闰六月）+ 全-range 5.4 万天逐日递推一致性；月视图截图确认农历行、休/班徽标、周末红字 |
| 2. 日程方块等额自适应 | `.cal-grid`/`.week-grid` 列行均 `minmax(0,1fr)` + 行高 `grid-auto-rows: minmax(0,1fr)` + 容器 `height: clamp(…100vh…)`；格内摘要 `line-clamp: 2` + `overflow:hidden`（换行、超出隐藏、绝不被内容撑大）；周视图展开态改卡内滚动 | 月视图截图验证；周视图 DOM 验证 7 卡片等分 |
| 3. 循环待办（每天/每周/每月） | 迁移 V2（recur_type/recur_anchor/recur_source_id）；`ensure_recurring` 补期引擎（幂等：保证每条链有未完成的 ≥今天 实例；每周保持周几、每月锚点几号月末截断）；启动 + 完成后 + 改动后 + 提醒调度器跨天四个触发点；完成/改动广播 todo.created；弹窗「重复」四选 + 列表 🔁 徽标 | Rust 单测（推算/月末截断/幂等/停止循环）+ e2e（追溯补齐/完成生成下一期/停止循环）+ 浏览器 UI 全链路（建每周待办→完成→自动出现下周五一期） |
| 4. 晨间简报→我的简报 + 今日热点 | 界面/模板/词条全量更名（内置 brief 模板升级到 v3，旧版进 STOCK_TEMPLATES_HISTORY 自动升级）；新增 `core/src/news.rs`：60s 开源 API 抓取（栏目注册表自动生成、4s 超时、逐栏目互不影响、30 分钟缓存、失败回退 stale 标记）；API `/news/channels` + `/news/hot`；简报卡片旁「今日热点」tag 切换面板；点击新闻桌面走系统浏览器/浏览器端开新标签；条数可配（5/10/15/20，后端上限 200）；栏目多选；**下滑自动加载更多**（滚动到底按页扩量，缓存覆盖判断保证翻页有效）；**自动更新开关 + 频率**（每 5/10/15/30 分钟/1 小时，面板打开期间按频率自动重抓） | 5 个单测（解析/合并/缓存/异常）+ e2e（栏目清单/结构/上限）+ 浏览器实测：10 条加载、滚到底自动增至 20、无降级横幅时不含提示、「更新于」时间显示、设置页五栏目与开关渲染 |

### 本轮断言（528 项全绿）

Rust 127 · 端到端 188 · 提醒 12 · 推送 20 · 桌面端静态 99 · 国际化 24 · 词条编译 10 · Markdown 渲染 17 · 农历节假日 31


---

## 十四、v1.1.0 轮次验收（排期迭代：删除语义 / 循环增强 / 记录热力图 / 速记聚焦）

| 需求 | 实现 | 验证 |
| --- | --- | --- |
| 删除待办（含循环语义） | 迁移前版本删除循环待办会被补期引擎"复活"。现 DELETE `/todos/{id}?scope=series` 一并软删根+全部实例并清空 recur_type（补期永不复活）；前端循环待办删除时二选一「删整个循环 / 仅此一条」，普通待办点删除后 3 秒内再点「确认删除？」防误删；今日页逾期列表开放操作按钮 | Rust 单测（链全删、删除后不再生成）+ e2e（scope=series 全消失）+ 守卫 2 条 |
| 循环待办增强 | 迁移 V3 加 recur_until / recur_interval / recur_skip_rest；生成规则支持每 N 天/周/月（月末截断不变）、截止日期到点整链停止、落点为休息日（周末/法定休假，Rust 侧假期表与前端 lunar.ts 同源维护）顺延到下一个工作日；弹窗可配 | Rust 单测 9 断言（间隔 2 周/15 天、截止停止、国庆顺延到 10-08、每 2 月、interval<1 收敛）+ e2e（截止日期循环创建/删除） |
| 记录热力图 | 新组件 HeatMap：近 26 周 GitHub 风格色块（列=周、行=周几），颜色深浅=当日记录数相对每日目标 5 档；点击色块打开当日详情抽屉；月视图新增卡片，显示活跃天数与总条数 | 浏览器 DOM 断言（35 格等高、同行布局）+ 截图确认 |
| 月视图速记聚焦 | 日详情抽屉打开即聚焦速记输入框（少一步点击） | 静态守卫 |

### 本轮断言（569 项全绿）

Rust 134 · 端到端 201 · 提醒 12 · 推送 20 · 桌面端静态 120 · 国际化 24 · 词条编译 10 · Markdown 渲染 17 · 农历节假日 31


---

## 十五、v1.1.1 轮次验收（排期迭代：自定义标签 / AI 打标 / 热点闭环 / 搜索源冗余）

| 需求 | 实现 | 验证 |
| --- | --- | --- |
| 自定义标签 | 新增标签中心 `stores/tags.ts`：选项 = 内置四枚 ∪ 用户自定义（settings.custom_tags）∪ 已在用（`GET /api/v1/tags` 统计记录+待办 JSON 标签合并计数、按次数降序）；速记 / 待办弹窗 / 节点编辑三处标签栏统一改用动态选项（此前各写死一份），均支持「＋」现场加词；设置页「🎯 每日目标」新增「自定义标签」卡片：增删自定义词（内置不可删、≤12 字）、展示使用次数 | Rust 单测（list_tags 合并去重排序）+ 守卫 3 条 + 浏览器实测（设置页加词 → 速记标签栏即时出现 → 删除消失） |
| AI 自动打标 | 新增 `POST /api/v1/ai/tag`：AI 从内容提取 0~3 个标签（优先复用用户已有标签、新造 ≤6 字、只输出 JSON 数组；`parse_tag_array` 容忍 ```json 包裹/废话/井号前缀/超长/去重/限量）；AI 未配置或解析失败降级为本地规则（已有标签名字出现在内容里才回填）；速记框内容停顿 2.5s（≥8 字）自动建议并选中（设置 `ai_autotag` 可关、内容变化丢弃过期响应、单飞在途请求），也可点「✨ AI 打标」手动触发；待办弹窗标题/描述可打标 | Rust 单测 5 断言（标准数组/代码块包裹/清洗去重限量/非数组为空）+ 守卫 2 条 + 浏览器实测 |
| 热点一键转记录/待办 | 今日热点条目悬停出现「📝 存为今日记录 / ✓ 建为待办」：转记录直接入库（标题+链接两行、打「热点」标签、防抖）；转待办打开新建弹窗预填标题/链接/标签（TodoEditModal 新增 preset prop），用户确认后入库 | 守卫 1 条 + 浏览器实测（转记录时间线出现、转待办弹窗预填） |
| 关键词搜索源冗余 | `news.rs` 搜索源注册表 SEARCH_ENGINES：必应（现行主源）→ 百度 → 搜狗，每源一个解析函数（新增 `parse_h3_results` 通吃 h3 型结果页、`absolutize_url` 补全搜狗相对链接）；`search_keyword` 从「上次成功的源」（进程级 LAST_GOOD_ENGINE 原子记忆）开始逐源尝试，请求失败/被反爬/解析为空自动切换下一个；`search_keywords` 加 40s 总时长预算（防源全挂拖死前端请求）；相关度打分跨源统一 | Rust 单测 5 断言（注册表约束/百度样例页/搜狗相对链接/各形态补全/跨源打分同口径）+ 守卫 1 条 |

### 本轮断言（586 项全绿）

Rust 143 · 端到端 201 · 提醒 12 · 推送 20 · 桌面端静态 128 · 国际化 24 · 词条编译 10 · Markdown 渲染 17 · 农历节假日 31


---

## 十六、v1.1.2 轮次验收（排期迭代：AI 智能速记 / 空状态引导 / 周视图自适应 / 月视图布局）

| 需求 | 实现 | 验证 |
| --- | --- | --- |
| 周视图内容自适应（用户反馈插入） | 用户反馈「还有大片空间却只显示 3 条」：废弃固定 FOLD_LIMIT=3 与 22 字截断，改为**全量渲染完整文本**（CSS overflow 裁剪兜底），用测量（getBoundingClientRect 逐行对比可视区，4px 容差）数出「真放不下」的条数——放得下就全部展示、不出按钮；放不下才出「＋N 更多」，底部渐隐遮罩提示还有内容；展开态卡内滚动不变；窗口尺寸变化（ResizeObserver 观察网格）自动重测 | 桌面守卫 2 条（无固定截断 + 测量驱动按钮）+ 浏览器实测 |
| AI 智能速记 | 速记行新增「🤖 拆待办」：一句话拆成待办（标题/日期/时间）。后端 `POST /api/v1/ai/extract-todos`：AI 配置了走模型（提示词带今天日期与周几，相对说法换算；`parse_todo_array` 清洗：标题≤60、日期必须合法、时间规整 HH:MM、≤5 条），模型没配/调用失败/格式跑偏**一律降级本地规则**（`core/src/todo_extract.rs`：今天/明天/后天/大后天/周X/下周X/下下周X/X月X日/X号/HH:MM/X点[半|一刻|三刻|N分] + 上午下午晚上中午修正 + 然后/接着/，再多意图拆分，标题剥祈使词）。前端 SmartTodoModal 逐条核对（勾选/标题/日期/时间均可改）后一键入库，打「智能速记」标签——拆解是建议，弹窗才是落库关 | Rust 单测 7 断言（相对日期/周几/下周/月日/多句/无日期退今天/模型输出清洗）+ e2e 6 断言（明天→15:00→标题、多意图、空内容报错）+ 守卫 3 条 + 浏览器实测 |
| 各页面空状态引导文案 | 周视图「未记录」一句话 →「这天还没有记录 · 月视图点这天可补录」；速记行 Enter 提示补 title「输入内容后可一键打标，或把一句话拆成待办」；待办/智伴/月抽屉空态此前已有 CTA，保持 | 守卫随 i18n 词条覆盖 |
| 月视图布局调整（用户反馈插入） | 记录热力卡片从月度小结下方移到**日程（日历）下方**：月度进度 → 本月小结 → 日历 → 记录热力 | 桌面守卫（模板顺序断言）+ 浏览器实测 |

### 本轮断言（607 项全绿）

Rust 150 · 端到端 210 · 提醒 12 · 推送 20 · 桌面端静态 133 · 国际化 24 · 词条编译 10 · Markdown 渲染 17 · 农历节假日 31


---

## 十七、v1.1.3 轮次验收（排期迭代：统一检索 / Ctrl+K 命令面板）

| 需求 | 实现 | 验证 |
| --- | --- | --- |
| 全局搜索升级 | `GET /api/v1/search`：记录+待办+报告一次查。过滤参数：kind（node/todo/report，空=全部）、tag（JSON 全词 LIKE）、from/to（记录按 date、待办按 due_date、报告按 created_at）、limit（每类上限 1-50，默认 20）；待办匹配标题+描述。报告**只回命中片段**（`make_snippet`：命中位置前后窗口、多字节安全、压单行带省略号）不回全文——报告动辄几千字，列表只要"它在哪、说了啥" | Rust 单测 5 断言（跨三类命中/类型过滤/标签过滤/日期范围/片段字节安全）+ e2e 8 断言（同词双命中/kind 过滤/标签排除/日期窗口/片段含词/缺 q 报错）+ 守卫 2 条 |
| Ctrl+K 命令面板 | 新组件 CommandPalette（App.vue 全局挂载，teardrop 到 body）：Ctrl/Cmd+K 呼出，顶栏 🔍 按钮可达；空查询显示 8 个快捷命令（六个页面跳转 + 新建记录 + 新建待办）；输入 250ms 防抖统一检索，结果按 记录/待办/报告 分组（每组各 10 条上限），命令始终置底可达；键盘 ↑↓ 选择、Enter 执行、Esc 关闭，高亮行自动滚入可视区；跳转语义：记录 → 月视图并打开该日抽屉（mindmate:open-day 事件，Month 切月+开抽屉）、待办 → 待办页、报告 → 智伴页；「新建待办」跨页事件（mindmate:new-todo → Todos 自动开弹窗）、「新建记录」→ 今日页聚焦速记框 | 守卫 2 条 + 浏览器实测（面板呼出/搜索命中/键盘导航/跨页跳转） |

### 本轮断言（626 项全绿）

Rust 155 · 端到端 220 · 提醒 12 · 推送 20 · 桌面端静态 137 · 国际化 24 · 词条编译 10 · Markdown 渲染 17 · 农历节假日 31
